on:
  push:
    branches:
      -master




jobs:
  build:      
    runs-on : ubuntu-latest

    steps:
      - name : 'psint folder cont'
        run: ls

      - name : 'Test Print'
        run : echo youssif

